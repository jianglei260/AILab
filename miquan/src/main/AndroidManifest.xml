<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miquan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MiQuanApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".ui.activitiy.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activitiy.DetailActivity"
            android:theme="@style/AppTheme.NoTitle" />
        <activity
            android:name=".ui.activitiy.ImageActivity"
            android:theme="@style/AppTheme.NoTitle" />
        <activity
            android:name=".ui.activitiy.ChargeActivity"
            android:theme="@style/AppTheme.NoTitle" />
        <activity
            android:name=".ui.activitiy.LoginActivity"
            android:theme="@style/AppTheme.NoTitle"></activity>
    </application>

</manifest>